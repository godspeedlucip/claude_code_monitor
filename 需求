可以做，建议方案是：**不要尝试从 IDE/CMD/终端窗口里“抓屏识别进度”**，而是利用 Claude Code 自带的 **statusLine + hooks** 把每个 Claude Code 会话的状态上报到一个本地服务，再由一个 Windows 悬浮窗统一展示。

## 最推荐架构

```text
任意入口启动 claude
VS Code Terminal / JetBrains Terminal / Windows Terminal / CMD / PowerShell
        │
        ▼
Claude Code 会话
        │
        ├─ statusLine：持续上报当前目录、模型、context 百分比、费用、耗时等
        ├─ hooks：上报 SessionStart / UserPromptSubmit / PreToolUse / PostToolUse / Stop 等事件
        ▼
本机 localhost 服务
Node.js / Python / Go，监听 127.0.0.1:4317
        ▼
悬浮窗 UI
Tauri / Electron / AutoHotkey GUI / WPF
```

Claude Code 的 status line 本来就是为“监控 context、费用、git 状态”等设计的：它会把 JSON session data 通过 stdin 传给你配置的脚本，然后显示脚本 stdout；并且会在 assistant 新消息、compact 完成、权限模式变化等时机更新。([Claude][1])
Hooks 则能在 Claude Code 生命周期事件触发时执行命令或 HTTP 请求，例如 session 开始、用户提交 prompt、工具调用前后、停止等；hook 输入里包含 `session_id`、`cwd`、`permission_mode` 等公共字段。([Claude][2])

## 能展示到什么程度？

悬浮窗里建议展示这些：

| 字段                   | 可行性 | 说明                                                  |
| -------------------- | --: | --------------------------------------------------- |
| 当前有哪些 Claude Code 会话 |   高 | 按 `session_id` 聚合                                   |
| 项目目录 / IDE 来源        |   高 | `cwd`、`workspace.current_dir`                       |
| 正在思考 / 正在调用工具 / 已停止  |   高 | 用 hooks 事件推断                                        |
| 当前 context 使用百分比     |   高 | statusLine JSON 有 context 信息                        |
| 当前模型、费用、耗时           |   高 | statusLine 官方支持这些展示场景                               |
| 正在读/改哪个文件            |  中高 | 从 `PreToolUse` / `PostToolUse` 的 tool input 提取      |
| 精确“完成百分比”            |   低 | Claude Code 没有天然暴露任务完成百分比，只能显示阶段、事件流、工具调用数、todo 变化等 |

所以你想要的“可视化其进度”，现实上应该做成 **任务流 / 状态面板**，而不是 0–100% 进度条。

## Windows 上的落地方式

Windows 原生支持 Claude Code；官方文档说明它可以在 Windows 10 1809+ / Windows Server 2019+ 运行，Shell 支持 Bash、Zsh、PowerShell、CMD。原生 Windows 下推荐安装 Git for Windows；如果没有 Git Bash，Claude Code 会回退到 PowerShell。([Claude][3])

你可以在 `C:\Users\<你>\.claude\settings.json` 里配置：

```json
{
  "statusLine": {
    "type": "command",
    "command": "powershell -NoProfile -ExecutionPolicy Bypass -File C:/Users/你的用户名/.claude/report-status.ps1"
  },
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "shell": "powershell",
            "command": "powershell -NoProfile -ExecutionPolicy Bypass -File C:/Users/你的用户名/.claude/report-hook.ps1 SessionStart"
          }
        ]
      }
    ],
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "shell": "powershell",
            "command": "powershell -NoProfile -ExecutionPolicy Bypass -File C:/Users/你的用户名/.claude/report-hook.ps1 UserPromptSubmit"
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": ".*",
        "hooks": [
          {
            "type": "command",
            "shell": "powershell",
            "command": "powershell -NoProfile -ExecutionPolicy Bypass -File C:/Users/你的用户名/.claude/report-hook.ps1 PreToolUse"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": ".*",
        "hooks": [
          {
            "type": "command",
            "shell": "powershell",
            "command": "powershell -NoProfile -ExecutionPolicy Bypass -File C:/Users/你的用户名/.claude/report-hook.ps1 PostToolUse"
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "shell": "powershell",
            "command": "powershell -NoProfile -ExecutionPolicy Bypass -File C:/Users/你的用户名/.claude/report-hook.ps1 Stop"
          }
        ]
      }
    ]
  }
}
```

Claude Code 的 Windows hooks 可以通过 `"shell": "powershell"` 指定 PowerShell 执行；官方也说明 hook 命令会从 stdin 收到 JSON。([Claude][2])
statusLine 在 Windows 上也可以显式调用 PowerShell 脚本。([Claude][1])

## 上报脚本示例

`C:\Users\你的用户名\.claude\report-status.ps1`

```powershell
$inputJson = [Console]::In.ReadToEnd()

try {
  Invoke-RestMethod `
    -Uri "http://127.0.0.1:4317/claude/status" `
    -Method Post `
    -ContentType "application/json" `
    -Body $inputJson `
    -TimeoutSec 1 | Out-Null
} catch {}

# 仍然给 Claude Code 自己的 status line 输出一点内容
try {
  $data = $inputJson | ConvertFrom-Json
  $model = $data.model.display_name
  $dir = Split-Path $data.workspace.current_dir -Leaf
  $pct = [int]($data.context_window.used_percentage)
  Write-Output "[$model] $dir | context ${pct}%"
} catch {
  Write-Output "Claude monitor active"
}
```

`C:\Users\你的用户名\.claude\report-hook.ps1`

```powershell
param(
  [string]$EventName
)

$inputJson = [Console]::In.ReadToEnd()

try {
  $obj = $inputJson | ConvertFrom-Json
  $obj | Add-Member -NotePropertyName "_event" -NotePropertyValue $EventName -Force
  $body = $obj | ConvertTo-Json -Depth 20 -Compress

  Invoke-RestMethod `
    -Uri "http://127.0.0.1:4317/claude/hook" `
    -Method Post `
    -ContentType "application/json" `
    -Body $body `
    -TimeoutSec 1 | Out-Null
} catch {}

exit 0
```

## 悬浮窗技术选型

我建议优先级：

1. **Tauri**：轻、适合常驻悬浮窗，Windows 体验好。
2. **Electron**：开发最快，Web UI 生态最舒服，但占用更高。
3. **WPF / WinUI 3**：Windows 原生感最好，但开发成本更高。
4. **AutoHotkey v2**：最快做 MVP，但复杂可视化能力一般。

悬浮窗 UI 需要做三件事：

```text
1. 本地 HTTP 服务收集 status/hook JSON
2. 按 session_id 维护会话状态
3. 窗口置顶 + 半透明 + 可拖拽 + 展示卡片
```

卡片可以长这样：

```text
Claude Code Monitor

● active  my-project
  C:\repo\my-project
  Sonnet 4.5 | context 37% | $0.42 | 12m
  当前阶段：PostToolUse: Edit
  最近工具：Read → Edit → Bash
  最后更新：3s ago

○ idle  backend-api
  D:\work\backend-api
  context 64%
  当前阶段：Stop
```

## 关键注意点

第一，**不要用窗口标题或进程名来判断任务进度**。从 CMD、PowerShell、IDE terminal 打开的 Claude Code 本质上都是终端里的 TUI，外部抓取不稳定。

第二，**最好用 `session_id` 聚合**。无论从哪个 IDE 或终端启动，只要 statusLine / hooks 都上报到同一个 localhost 服务，悬浮窗就能统一管理。

第三，**不要把完整 prompt、文件内容、diff 直接上报到悬浮窗日志**。Hooks 有权限读到相当多上下文；官方也提醒 command hooks 以当前用户权限运行，能访问用户可访问的文件，所以脚本要谨慎处理输入、避免上传敏感内容。([Claude][2])

第四，**“进度条”建议改成“阶段 + 活动流”**。Claude Code 没有稳定的总任务长度，百分比容易误导；更可靠的是显示：当前会话是否活跃、最近工具调用、context 使用、最后事件、是否已 Stop。

## 我的建议结论

你要的效果完全可以实现，最佳路线是：

**全局 `~/.claude/settings.json` 配置 statusLine + hooks → PowerShell 脚本上报到 localhost → Tauri/Electron 悬浮窗展示多个 session。**

这样不管你是在 VS Code Terminal、JetBrains Terminal、Windows Terminal、CMD、PowerShell 里打开 Claude Code，都会自动出现在同一个悬浮窗里。

[1]: https://code.claude.com/docs/en/statusline "Customize your status line - Claude Code Docs"
[2]: https://code.claude.com/docs/en/hooks "Hooks reference - Claude Code Docs"
[3]: https://code.claude.com/docs/en/setup "Advanced setup - Claude Code Docs"
